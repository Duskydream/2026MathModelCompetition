"""Build the preliminary Chinese report from verified computation outputs."""
from pathlib import Path
import csv,json,hashlib

B=Path(__file__).resolve().parent;R=B/'results'
s=json.loads((R/'summary.json').read_text());a=json.loads((R/'audit_checks.json').read_text());g=json.loads((R/'geometry.json').read_text())
rows=[]
for v in s['main']:
 rows.append(f"| 问题{v['question']} | {'B0 单次测向＋光学覆盖' if v['strategy']=='sweep' else 'B1 两次测向＋光学覆盖'} | {v['cases']} | {v['total_sources']} | {v['clearance_ratio']:.2%} | {v['mean_case_average_s']:.2f} | {v['p95_case_average_s']:.2f} |")
table='\n'.join(rows)
paired='\n'.join(f"- 问题{v['question']}：平均减少 {v['mean_saved_s']:.2f} 秒/源，相对下降 {v['relative_mean_reduction']:.2%}；配对 bootstrap 95% 区间为 [{v['bootstrap_95_percentile_ci_s'][0]:.2f}, {v['bootstrap_95_percentile_ci_s'][1]:.2f}] 秒/源；40 对中 {v['improved_cases']} 对改善。" for v in s['paired'])
candidates=list(csv.DictReader((R/'second_point_candidates.csv').open(encoding='utf-8-sig')))
candidate_table='\n'.join(f"| {v['forward_m']} | {v['lateral_m']} | {float(v['move_s']):.2f} | {float(v['sampled_worst_diameter_m']):.2f} |" for v in candidates if (v['forward_m'],v['lateral_m']) in [('250','100'),('500','250'),('650','250'),('750','450')])
stress=list(csv.DictReader((R/'stress_sensitivity.csv').open(encoding='utf-8-sig')))
sens=[]
for e in ['0.5','1','1.5']:
 vals=[]
 for q in ['3','4']:
  rs=[r for r in stress if r['group']=='sensitivity' and r['question']==q and r['amplitude_deg']==e]
  vals.append(sum(float(r['average_per_source_s']) for r in rs)/len(rs))
 sens.append(f"| ±{e}° | {vals[0]:.2f} | {vals[1]:.2f} | 全部清除 |")
text=r'''# B题审题 基准模型与验证方案

本报告完成第一阶段的审题、参数核查、可运行基准建模及本地验证。建议以“有界误差可行域＋具有覆盖保证的搜索＋光学覆盖兜底”为主线，采用两次测向的 B1 作为后续优化起点。四份原文件均已读取；题目 PDF 的四页已逐页对照图文。两份附件通过 OOXML 读取全部正文、15 张表和28个公式对象；旧版 DOC 通过复合文档及分段文本表解析读取，未修改原文件。

**结果性质：本报告中的试验全部由本地自建模拟器产生，不是官方演练，更不是正式测试。官方案例编码、成绩和加密日志目前均不存在，不能用本地结果代填。** 本阶段交付审题与基准研究报告，不作为最终参赛论文。

## 一 文件与信息核查

| 文件 | 实际内容 | 建模用途 |
|---|---|---|
| B题.pdf | 4页题目、四问、附录1至4 | 几何、物理规则、评价与测试要求 |
| 附件1.docx | 行为与计时说明、2张表、模拟器操作说明 | 指令状态、案例操作、正式日志要求 |
| 附件2.docx | HTTP＋JSON协议、13张表、Python/Matlab示例 | 可观测信息、错误处理、幂等与时间限制 |
| format2026.doc | 2026版论文规范、承诺书、编号页 | 后续论文与支撑材料组织 |

输入文件的大小和 SHA-256 已保存在 `source_extract/manifest.json`，正文提取件位于同目录。两份 DOCX 无媒体文件、无修订插入或删除。附件中的示例代码被 Word 排版合并，不直接复制为算法；依据协议另行编写客户端。

没有 CSV、Excel 或源坐标观测表。这里的“未知”包括源数量、频道集合、坐标、接收半径和定向参数，是题目有意隐藏的状态，**不能当成普通缺失值补均值，也不能让策略读取本地模拟器的真值**。第一、二问没有给出具体检测数据，因此给出的数值几何案例均明确为自构算例。正式测试表的空白是待测试结果，不是可填补的数据缺口。

### 题目事实与单位

| 项目 | 事实与单位 | 来源 |
|---|---|---|
| 源区域 | 圆心(0,0)，半径1800 m；东为x正向、北为y正向 | [1]第1页 |
| 数量 | 第三、四问为10至16个；数量不通过接口返回 | [1]第1至2页；[3]1.3 |
| 频道 | 整数1至20，一源一频道，互不相同且不变 | [1]附录1 |
| 方向 | 东0°、北90°，逆时针，返回范围[0°,360°) | [1]附录2；[3]1.2 |
| 误差 | 全局有界±1°；同地点重复检测误差固定 | [1]第2页 |
| 返回精度 | 示向度保留两位小数 | [3]7.2至7.3 |
| 接收半径 | 每个源在1000至1500 m之间，具体值未知 | [1]第3页；[3]2.1 |
| 定向辐射 | 发射方向两侧各90°，含边界，即闭半平面方向约束 | [1]附录1 |
| 移动 | 直线，5 m/s，可走出源区域，坐标分量绝对值≤2000000 m | [1]第3至4页；[3]1.1 |
| 检测与切换 | 每次检测5 s，换检测频道另加1 s | [1]附录2；[3]4.3 |
| 光学与清除 | 距源≤20 m即成功，不受定向辐射限制；成功5 s，失败3 s | [3]8.2及4.4 |
| 近距离 | 距源≤5 m且能收到信号时返回near，可原地清除 | [1]附录2(9) |
| 时限 | 虚拟时间360000 s；现实时间最多1200 s且受25分钟窗口约束 | [1]第4页；[3]4.5 |

所有内部三角函数使用弧度；接口输入输出保持度和米。不能把通常以北为零顺时针的航向角约定带入本题。分钟仅在解释现实测试窗口时使用。

### 建模假设

1. 接收、近距离、清除均严格依照附件的确定性阈值；不另外假设遮挡、运动障碍、信号丢包或源移动。
2. 源位置在一次任务中固定。题目静态案例与交会定位方法支持这一解释；若存在移动源，当前可行域累积方法须重建。
3. 算法采用确定性的有界误差模型，不依赖独立、正态或零均值。为了兼容“误差先产生、再四舍五入”的可能实现，程序使用±1.005°；如果官方±1°已包含取整，这只是保守放宽。
4. **仅限本地生成器**：数量在10至16均匀抽取；位置在圆盘按面积均匀生成；频道无放回抽样；接收半径均匀生成；第四问每个源以0.5概率为定向源且方向均匀。官方分布未公开，这些不是题目事实。
5. 本地误差通过种子、频道、坐标的 SHA-256 确定，重复坐标得到同值；另测常量与光滑空间偏差。该构造只供可重复试验，不宣称复现官方电磁环境。

### 待核实与暂未具备的信息

官方模拟器可从[2]4节提供的百度网盘地址获得，但文件夹中没有其程序及任何案例日志；注册登录需要队伍自行操作。当前没有启动官方测试，也没有消耗正式机会。后续需核对实际端口、登录状态、联网表现、官方误差取整语义及演练结果。题目给出的截止时间为北京时间2026年9月13日17:30，建议正式测试在15:30前完成[1][2]。格式规范还引用了一份独立的《人工智能工具使用规定》，本目录未提供该文件，最终提交前需核对。

这些缺项不阻碍当前基准建模，因此本阶段不以其为由停下等待。

## 二 四问的目标与相互关系

| 问题 | 输入与决策 | 目标 | 必须处理的约束或陷阱 |
|---|---|---|---|
| 1 | 检测点、示向度；计算交会可行域 | 算出直径，回答同直径圆能否覆盖 | 半直线而非无限直线；空域、无界、退化；区域直径≠包围圆直径 |
| 2 | 已有一次全向源示向度；选择第二点 | 在移动代价与定位精度间取舍 | 源距离未知；第二点需收到信号；不能保证所有源都形成90°交会 |
| 3 | 单狗在线选择位置、频道、测向或清除 | 先确保全部清除，再缩短虚拟总时间 | 不知道真实总数；检测不并行；停止须有证据 |
| 4 | 再加入未知源类型与定向方向 | 保持清除保障并提高效率 | 无信号不能按距离直接排除；边界朝外源需边界或区外测站 |

第三、四问是部分可观测的在线搜索与控制问题，不是已知坐标的旅行商问题。可借鉴路径优化，但必须先解决“哪里还可能有未发现的源”和“何时能结束”。目标写为在全部清除条件下最小化

\[
T=\frac{L}{5}+N_{\rm switch}+5N_{\rm measure}+3N_{\rm clear\ attempt}+2N_{\rm success}.
\]

最后两项已经包含光学定位与激光清除，不能再额外加一遍3秒。`/clear`的频道用于选目标，不更新测向机频道；`/enter`、`/exit`不增加虚拟时间。

## 三 第一问 有界误差交会与直径

令第i个检测点为sᵢ，读数θᵢ，误差界ε。定义u(α)=(cosα,sinα)，二维叉积[a,b]=aₓbᵧ−aᵧbₓ。可行楔形为

\[
W_i=\{x:[u(\theta_i-\varepsilon),x-s_i]\ge0,
[u(\theta_i+\varepsilon),x-s_i]\le0\},\qquad
P=\bigcap_i W_i.
\]

每个楔形贡献两条线性半平面约束Ax≤b。2ε小于180°，因此约束已经选定指向源的前向楔形；不能只计算误差直线带而包含背向位置。

### 可运行基准算法

1. 用线性规划判定可行性。不可行则报告空集和数据/误差矛盾，不输出伪定位点。
2. 分别求x、−x、y、−y四个线性目标，判定是否无界。无界区域直径为无穷，不能用一个人为大方框截断后冒充真实直径。
3. 对所有非平行边界线两两求交，只保留满足全部不等式的点，去重后按凸多边形顺序排列。
4. 枚举顶点对，计算D=max‖vᵢ−vⱼ‖。凸多边形的任意点是顶点凸组合，由距离的凸性，最远点对可取在顶点上。
5. 对线段和单点保留其退化直径。本实现同时验证了空集、无界、线段、单点情况。

若有m条约束，直观顶点枚举最坏O(m³)，直径为O(k²)。本题单源测站很少，该基准足够。大量测站时可改用半平面交加旋转卡壳，但不是当前必要条件。

若额外加入精确圆盘先验，交集可能有圆弧，不再是纯多边形；当前第一问回答题目所指的楔形交会多边形。在线策略使用其保守外包多边形，保证不会排除真源。

### 同直径圆一般不能覆盖

考虑边长40 m的正三角形：D=40 m，但其最小包围圆半径为40/√3=**@@RADIUS@@ m**，大于D/2=20 m。这不只是随意的多边形反例：将各边起点沿边反向延伸1100 m设置检测点，令示向度为该边方向加1°，每个楔形的一条边即三角形的支持边，另一条边不切掉三角形；三楔形交集就是该三角形。具体站点、读数和计算顶点保存在 `results/geometry.json`。

因此“D≤40 m就能一次清除”是错误的。正确条件是

\[
R_* = \min_c\max_{x\in P}\|x-c\|\le20\text{ m}.
\]

对凸多边形只需检查顶点，最小包围圆由1、2或3个边界顶点确定。本实现枚举这些支持集合，直接计算圆心和半径。图 `results/q1_counterexample.png` 来自上述真实计算。

## 四 第二问 第二检测点的候选区域

以首站为原点，把读数方向旋转为局部x轴；设源位置g=r(cosδ,sinδ)，0≤r≤1500、|δ|≤ε。全向源首次可见说明其真实接收半径R≥max(1000,r)。对于第二点q=(a,b)，一个不依赖R和r的**充分候选区域**是

\[
\mathcal C_{safe}=\{(a,b):a^2+b^2\le1000^2,
\quad a^2+b^2\le2000(a\cos\varepsilon-|b|\sin\varepsilon)\}.
\]

这是保守的、可计算的保证接收区域，不宣称包含全部可选点。转换回原坐标为q=s₁+a u(θ₁)+b u(θ₁+90°)。允许点在1800 m圆域之外，因为附件允许机器狗离开源区域。

证明：当r≤1000时，固定δ的距离平方关于r是凸函数，其最大值在0或1000处；上述两个不等式分别保证两端距离不超过1000。当r≥1000时，需要‖q−ruδ‖²≤r²，即‖q‖²≤2r q·uδ；右端在r=1000且δ取使点积最小的端点时最紧。因此两类距离均满足实际接收半径条件。候选式对应的点积为正；|δ|≤ε时最小点积为a cosε−|b| sinε。

不能只说“使两条射线正交”，因为首测没有距离，且为某个估计源构造的正交站可能看不见真实源。近似局部信息矩阵可写为Σᵢ(1/rᵢ²)nᵢnᵢᵀ，提示应避免共线，但本题没有误差概率分布，这里只作为几何解释，不能直接把逆矩阵当成已验证置信区间。

本阶段采用a=650 m、b=250 m的固定、可解释折中方案，也允许镜像b=−250 m；代码固定正侧以保证复现。选择理由是保证收到全向信号、具有横向基线，移动时间适中。它是比较基准，不宣称最优。

用21个距离、3个首测偏差方向、3个第二测角度偏差组合，对12个候选点逐一计算交会区域。每点189个组合，代表性结果如下；“最坏”仅指该离散集合，不是连续域最坏值的数学证明。

| 前进a / m | 侧移b / m | 首站至第二点移动 / s | 样本内最大定位区域直径 / m |
|---|---|---|---|
@@CANDIDATES@@

更大的横向基线与前进距离降低了样本内最坏直径，但增大移动代价。后续应以“移动＋检测＋剩余光学覆盖”的最坏时间或稳健分位时间选择点，而不是只最小化区域面积。可在上述区域上粗网格筛选，再对优良候选作细化及区间验证；未完成连续全局优化前不声称最优。

## 五 第三问 全向源基准策略与终止证明

### 发现阶段

使用步长1000 m的方格测站，两个坐标分别取−2000、−1000、0、1000、2000，共25站。每个源到某个网格点的距离≤1000/√2≈707.11 m，小于最小接收半径1000 m，故全向源一定被某站发现。离开源圆域的测站合法。

从原点开始，每轮选择离当前狗位置最近的未访问测站。到站后检测所有尚未成功清除的频道，优先保持当前频道，其他频道递增排列。**先完成本站频道扫描，再逐个处理可见目标**，避免为每个命中频道往返一次测站。此版目标处理顺序仍按发现顺序，尚未优化多目标访问路线。

### B0 单次测向加光学覆盖

一次有效读数给出长度不超过1500 m、角宽2ε的楔形。用局部三角形

\[
P_0=\operatorname{conv}\{(0,0),(1500,-1500\tan\varepsilon),(1500,1500\tan\varepsilon)\}
\]

外包真实扇区；不需要猜测源距离，也不需要假设高斯误差。计算包围圆，若半径≤20 m则直接去圆心清除；否则用25 m方格覆盖P₀，保留与P₀相交的方格，在各格中心依次尝试清除。任意格内点到中心≤25/√2=**@@CELLR@@ m**，因此覆盖必能包含真实源。采用按列蛇形遍历，可正向或反向，以离当前点较近的端点开始。成功后立即结束该频道的定位。

一次清除失败花3秒，不能忽略。该策略较慢，但优点是几何保证清楚；即使第四问之后再也收不到该源，也能靠已获得的一次方位完成光学清除。

### B1 两次测向加同一兜底

由首站移动至第二点(650,250)的局部位置再检测。若返回direction，将第二楔形与P₀相交，再计算包围圆或覆盖剩余小多边形；若返回near则原地清除；第四问若返回no_signal，则保留第一次的P₀并执行同一光学兜底。不把定向源的一次失联误当成源消失。

初次或第二次返回near时，不需要再交会。若本应包围真源的集合为空，或有保证的清除失败，程序报错留存日志，不以外推坐标掩盖矛盾。

### 知道什么时候结束

出现以下任一条件才正常结束：

- 已成功清除16个源，达到题目上界。
- 所有覆盖测站已访问；每个未清除频道在这些站都被检测过；所有发现的目标均已清除。

由于覆盖保证，第二种情形不可能还存在未发现源。“清除了10个”“连续多次没收到”“已扫描全部20个频道一次”都不是有效终止证明。

## 六 第四问 任意方向源的覆盖保证

采用间距h=600 m的方格顶点，坐标取−1800至1800的7个值，共49站。源圆盘包含在该正方形内，每个源g都能选定一个包含它的闭方格，边界点可选相邻格。四个顶点v₁至v₄到g的距离均≤√2h≈848.53 m，小于最小接收半径。

设未知发射方向单位向量为d。g在四顶点凸包内，存在非负λᵢ且Σλᵢ=1使g=Σλᵢvᵢ。因此

\[
\sum_i\lambda_i d^T(v_i-g)=0.
\]

至少一个顶点满足dᵀ(vᵢ−g)≥0，即落在含边界的发射半平面内，同时在接收半径内，故可见。这个论证对区域边缘朝外的源仍然成立；边界被题目明确包含是证明的一部分。

这解释了为什么第三问“每个源附近有一个测站”的条件不能直接用于第四问：该最近站可能在所有信号的背面。更一般地，要求源位于若干个近距离测站的凸包内，才能对任意未知朝向给出保证。

第四问其他扫描、定位和结束流程沿用B1。第二点的全向接收证明不能用于定向源，因而必须保留光学兜底。本阶段没有试图从一次no_signal估计或删除发射方向。

### 有限步与虚拟时间上界

在默认ε=1.005°、光学格距25 m下，局部候选格中心只需61个横向列、至多3个纵向行，故≤183格。实际完整P₀经相交筛选后为 **@@CELLS@@ 格**，蛇形路径长度为 **@@SNAKE@@ m**；对任意交会后凸子集，按列路线用6500 m作保守上界：每列内部移动≤50 m，相邻非空列间距离≤√(25²+50²) m，连续凸域不会跳过内部列。

所有测站及定位清除点都位于距原点4400 m以内，所以任意两行动点之间距离≤8800 m。用49个测站、每站20次检测含切换最多120 s，以及16个目标、每目标最多两段全局跳转、一次新增检测和183次清除，得到粗上界

\[
T\le49(8800/5)+49(120)+16[(2\times8800+6500)/5+6+183\times3+2]
=\mathbf{@@BOUND@@}\text{ s}<360000\text{ s}.
\]

这是默认基准下的宽松虚拟时间保证，不是平均效率估计；它不能证明真实HTTP通信一定在20分钟内结束。客户端按照`remaining_real_duration_s`设置现实截止时间；网络开销仍需官方演练检查。

## 七 本地验证结果

### 试验设计与隔离

随机种子20260910。第三、四问各生成40个独立案例；同一问同一案例分别运行B0和B1以配对比较，共160次主试验。每组40例共有498个源。生成器持有真值，策略仅调用`measure`和`clear`获取反馈；真值用于事后的验证与评分，不用于选择动作。

另做60次压力测试与30次误差幅度敏感性测试，总计250次。本地逐指令日志、每次配置、源真值与定位区域保存在`results/local_runs.jsonl.gz`；它是自建环境日志，不是官方加密日志。

### 配对主试验

表中时间为“先按每个案例计算T/成功清除数，再对40例平均”，单位是虚拟秒/源。P95同样对案例平均时间取分位；它不是官方未知分布的保证。清除比例按累计已清除数/累计总数计算；本实验全部成功，因此与各案例比例平均一致。

| 题型 | 策略 | 案例数 | 每组源总数 | 清除比例 | 平均虚拟秒/源 | P95虚拟秒/源 |
|---|---|---|---|---|---|---|
@@TABLE@@

@@PAIRED@@

bootstrap以案例为单位进行5000次重采样，种子20269910；区间描述所设本地案例分布下的采样不确定性，不能解释为官方比赛效率区间。第四问有3例B1更慢，说明固定第二测站并非逐实例占优。

B1在第三问平均每源移动耗时626.24 s，第四问779.64 s，分别明显超过其他单项。因此下阶段主攻减少移动与重复排查，而不是增加复杂估计器。完整耗时分解见`results/audit_checks.json`。

图表见`results/comparison.png`。所有图中值均由`cases.csv`计算，图注明确标注本地合成试验。运行耗时记录在CSV，仅为当前机器上的本地程序时间，不能填写正式测试表的程序运行时间。

### 约束与误差检查

- 250次本地运行全部完成清除，全部源数量、频道唯一性、区域边界和接收半径检查通过；合成数据必填字段缺失数为0。定向方向为null表示全向，这是类型语义，不是待补缺失值。
- 对每条指令重新累加直线移动、换频道、检测、光学和激光时间，与日志累计时钟差异要求≤10⁻⁶ s。
- 每次成功清除都核验实际距离≤20 m，无重复清除成功；所有保存的定位多边形均含真源，包围圆均覆盖真源。
- 测试附件示例得到199 s；专门检查`/clear`不切频道、同地点误差重复、0°/360°归一化、1000 m接收边界、5 m近场边界、20 m背向光学清除，以及空集、无界、线段与单点几何。
- HTTP适配器通过3项不联网单元测试：超时重试复用相同请求体/ID、拒绝响应不把时钟重置为0、按实际剩余时间设置期限。它尚未经官方服务器联调。

误差指标以“真源是否仍在集合内”“是否超出保证清除距离”为主。没有擅自报告高斯RMSE或置信椭圆，因为题目仅给有界误差；未来若使用点估计，可另统计点到真源的误差，但不能代替包含性与清除成功判据。

### 稳健性与敏感性

压力测试覆盖边界源且最小半径、中心聚簇、全体最小接收半径、第四问全部定向朝外、固定±1°偏差、光滑空间相关偏差与零误差，每类每问5例，共60次，全部清除。第三问的“all_directional”标签只是复用生成器场景名，仍全为全向源，实际对应固定−1°偏差检验。

另外改变实际误差幅度并同步放宽算法边界为幅度＋0.005°，每档每问使用相同5个种子比较：

| 误差幅度 | 问题3平均秒/源 | 问题4平均秒/源 | 结果 |
|---|---|---|---|
@@SENS@@

±1.5°是超出题目规则的压力场景，且此表是“正确知道更大误差界”的敏感性分析，不是低估误差仍能稳健工作的证据。专门构造真实偏离1.5°而只容许1.005°的算例，得到正的半平面违约 **@@VIOLATION@@ m**，证明误差界失配可能将真源排除。正式演练一旦出现空交集或保证清除失败，应先排查取整、角度方向、坐标、计时和模型假设，不能静默改数据。

## 八 后续方案与创新点的核验路线

### 优先一 用“剩余可行状态”减少全域排查

为每个未清除频道维护可行位置集。第三问一次no_signal可排除以检测点为圆心、半径1000 m的闭圆盘；一次direction提供角度约束和距离≤1500 m。一旦区域与全部约束的交为空，即可证明该频道不存在未清除源。不要以固定次数“未检出”替代空集证明。数值实现可从四叉树格块开始，只有整个格块被严谨排除时才删除。

第四问需同时维护位置g、朝向d、接收半径R的可行状态。正检测要求距离≤R且dᵀ(s−g)≥0；负检测是距离>R或背向的析取，不能只在平面上减一个圆盘。可使用位置格块＋朝向区间的保守表示，并用近距离测站的凸包覆盖证书排除“还有某个方向看不到”的情况。该方法有创新空间，价值在更快取得终止证明；必须逐步验证绝不错误删掉真状态。

### 优先二 按完成任务所需时间选择测站

将第二点、第三点选择改成小规模滚动决策，候选动作包括测向、直接光学清除和当前可行域覆盖。比较“移动/5＋检测和切换时间＋剩余保守清除时间”，而不是单看交角、熵或面积。若已有包围圆半径≤20 m，优先清除，无须继续追求更高定位精度。

这里的适应性只在有限候选中选点即可，不需要强化学习。对全向源候选使用已证明的接收区域；对定向源保留失联分支代价。先在未用于选参数的新增种子上检验时间和清除率，再决定是否替换固定(650,250)方案。

### 优先三 多目标共站与局部路径优化

一次移动可以使多个频道都获得更好几何关系。让第二测站兼顾多个已有可行域，优先就近完成已可安全清除的源，并对一批目标顺序做最近邻或2-opt；不强制清除后回到原测站。本基准仍有扫描后按频道次序逐一处理的折返，耗时分解已表明值得改善。

采用消融比较：B1、B1＋排除证书、B1＋共站/路线、三者组合；固定同一批验证案例、相同误差场与随机种子。只有在不破坏覆盖和包围条件、且独立验证集改善稳定时，才保留改动。

### 官方演练与正式测试

完成官方模拟器安装登录后，先在第三、四问演练模块分别验证HTTP、计算时钟和清除数，核对自录日志与官方结果。无次数限制的演练用于查错和验证改进，不用正式测试调参。充分演练后，每问按题目各做三次正式测试，保留原名加密日志并填写题目表1。三次正式样本很少，不能以其声称普遍最优。

本次没有下载或运行官方模拟器，没有读取隐藏状态、没有生成案例编码，没有正式测试日志。`http_robot.py`已准备串行适配器，但其存在不代表完成官方测试。

## 九 适用范围与局限

几何包含性和全清除证明适用于静止源、规定的接收范围与闭180°方向覆盖、有界测向误差、无障碍直线行走及确定性20 m清除机制。当前格点搜索较保守，B1不是时间全局最优策略。最坏虚拟时间保证不覆盖网络中断导致的现实超时。算法没有获取信号场强或真实距离，因为接口只返回状态与示向度。

本地模拟器按浮点秒累计，官方按微秒累计并保留最多六位小数；阈值数值容差也可能不同。边界证明是精确数学条件，官方联调时需检查浮点边界行为。合成分布、定向比例、误差空间机制均是假设；主试验100%成功不能单独证明普适性，普适性来自上述覆盖与光学兜底论证，并依赖前述物理与数值条件。

最终论文应在完成官方演练与正式日志核验后写作。按[4]：摘要含标题关键词原则上≤1页；正文无目录且≤30页；附录含全部可运行代码和支撑文件列表；电子版不含承诺书与编号页，首为摘要页；页边距至少2.5 cm；论文与支撑压缩包分别≤20 MB；匿名范围覆盖正文、附录与支撑文件。当前报告不冒充最终排版论文。

## 十 引用与可复现材料

[1] 用户提供的《2026年高教社杯全国大学生数学建模竞赛 B题 无线电干扰源的快速自动定位与清除》，`Question B/B题.pdf`，4页。

[2] 用户提供的《模拟器使用说明》，`Question B/附件/附件1.docx`，第1至4节。

[3] 用户提供的《模拟器通信接口说明及编程指南》，`Question B/附件/附件2.docx`，第1至12节。

[4] 用户提供的《全国大学生数学建模竞赛论文格式规范（2026年修订稿）》，`Question B/format2026.doc`。

本文几何结论给出自足推导，未引入无法核验的外部文献。原文件SHA-256可按manifest核对。运行命令、参数、软件版本、测试说明和文件清单见`README.md`；主试验表、候选表、压力试验表均保存逐例数值，图表和本报告可由保存的脚本重建。
'''
for key,value in {'TABLE':table,'PAIRED':paired,'CANDIDATES':candidate_table,'SENS':'\n'.join(sens),'RADIUS':f"{g['minimum_circle_radius_m']:.6f}",'CELLR':f"{a['optical_cell_circumradius_m']:.6f}",'CELLS':str(a['full_first_polygon_actual_cells']),'SNAKE':f"{a['full_first_polygon_actual_snake_path_m']:.3f}",'BOUND':f"{a['analytic_virtual_time_bound_s']:.0f}",'VIOLATION':f"{a['understated_error_counterexample_halfplane_violation_m']:.6f}"}.items():text=text.replace('@@'+key+'@@',value)
assert '@@' not in text
(B/'审题与基准模型报告.md').write_text(text,encoding='utf-8')
env=json.loads((R/'environment.json').read_text());env['code_sha256']={p.name:hashlib.sha256(p.read_bytes()).hexdigest() for p in B.glob('*.py')};(R/'environment.json').write_text(json.dumps(env,ensure_ascii=False,indent=2),encoding='utf-8')
print('Report generated from verified results.')
