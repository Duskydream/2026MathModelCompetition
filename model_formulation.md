# 数学模型与保证条件

本轮只修改第三问搜索点集合；下文区分已实现策略、连续证明和离散评估。长度用米，角度接口用度，三角函数用弧度。

## 变量与目标

区域为B(0,R)，R=1800；源g_j、频道c_j、接收半径r_j∈[1000,1500]、第四问发射单位向量d_j未知。机器狗动作a_t=(类型,s_t,c_t)，测向频道k_t仅由成功接受的measure更新。成功清除集合C必须包含全部源，然后最小化

\[
T=L/5+N_{switch}+5N_{measure}+3N_{clear}+2N_{success},\qquad \bar T=T/N_{success}.
\]

实际源数量未知，不能由计时指标推断清除比例。离线评分器可读真值，策略只能读动作反馈。无信号不是无源的充分条件。

## 第一问

令u(θ)=(cosθ,sinθ)，[a,b]=a_xb_y−a_yb_x。每个读数定义

\[
W_i=\{g:[u(\theta_i-\epsilon),g-s_i]\ge0,\quad
[u(\theta_i+\epsilon),g-s_i]\le0\}.
\]

题目几何取ε=1°；在线程序用1.005°保守包括两位小数取整。求P=∩W_i：先LP判断空/无界，再枚举所有满足约束的边界交点。D=max_{v_i,v_j}‖v_i−v_j‖。凸性保证极值可在顶点对取得。

边长40的正三角形D=40，却有最小包围圆半径40/√3>20；三组楔形能实际构造该三角形，测试已验证。故同直径圆一般不能覆盖。若最小包围圆由一对直径端点支撑，半径恰为D/2时才成立。

在线保证清除使用R*=min_c max_{v∈vertices(P)}‖v−c‖≤20−1e−6。MEC枚举1/2/3点支持候选；每个候选重新检查全部顶点。最坏O(k^4)，现行k很小。

## 第二问

首站局部坐标：q=(a,b)，源g=r(cosδ,sinδ)，|δ|≤ε。首测可见意味着真实接收半径≥max(1000,r)。充分接收候选区域为

\[
\mathcal C=\{(a,b):a^2+b^2\le10^6,\quad
a^2+b^2\le2000(a\cos\epsilon-|b|\sin\epsilon)\}.
\]

当r≤1000，固定δ的距离平方关于r凸，检查端点0、1000即可；当r≥1000，‖q−ruδ‖≤r等价于‖q‖²≤2r q·uδ，最紧情形由1000与最小点积给出。区域可旋转平移到任意首站。目标圆先验还能缩小可行域；当前运行代码使用1500米扇区的三角形外包，未用圆盘裁剪，安全但保守。

候选点应在移动成本、最坏定位面积/直径/MEC半径和接收可靠性之间取舍。理想形式为在C内对所有合法源与第二次误差，最小化后验MEC半径与移动耗时的Pareto目标，或以完整后续动作成本加权。未验证权重之前不声称固定650/250为最优。

本轮补齐12候选点×189情景的面积、直径、MEC、交角与移动数据，见`experiments/round1/q2_candidates.csv`和`q2_scenarios.csv`。这些是离散几何情景，不是官方随机样本；接收“情景比例”不是概率。图中的候选区域由上述不等式定义，非只给单一点。本轮未改变第二站策略，未开展权重或自适应步长调参。

## 第三问七站连续覆盖证明

取O=(0,0)，ρ=Rcos30°=900√3，S_k=ρu(60°k)，k=0,…,5。

对任意P=tu(φ)，0≤t≤R：若t≤R/2，则‖P−O‖≤R/2。否则选择与φ相差δ≤30°的最近环点，则

\[
\|P-S_k\|^2=t^2+\rho^2-2t\rho\cos\delta
\le t^2+\rho^2-2t\rho\cos30^\circ=f(t).
\]

f在[R/2,R]凸，两端值均为R²/4，故区间内不超过R²/4。于是全圆最近站距离≤R/2=900<1000。P处于半径900或1800且位于相邻环点的角平分线上时可取等号，故最大值确为900。无需Monte Carlo推断。

原25格点方案最大最近距离1000/√2≈707.107，两者都满足保证。7站没有被证明全局站数最少或路径最优。环半径来自解析等化端点误差，不是对随机种子调参。数值验证包含5米笛卡尔网格及圆周14401点，共421474点。

每个剩余频道必须在所有搜索站被扫描；已发现源逐个完成清除。因此站点耗尽时不存在未发现的全向源。已清除16个则可用数量上界结束。只知道清除10个不能结束。

## 第四问方向覆盖证明

保持49站600米方格。圆内源属于某一闭格，四顶点距离≤600√2<1000；g=Σλ_i v_i，λ_i≥0、Σλ_i=1。任意d都有Σλ_i d·(v_i−g)=0，故至少一项非负，可见半平面含边界，必能发现。该证明对边界点成立，源在凸包闭包即可，不要求严格内部。

数值程序独立检查：取距每个样本点≤1000的所有测站，将观察方向排序，最大循环角间隔≤180°。方向间隔检验辅以解析证明，不把有限采样当普遍保证。圆外辅助站不得随意删除。

7站不能用于第四问：源g=(1800,0)，朝向正东，所有7站均在背侧，全部无信号。该反例已运行并存入coverage_geometry.json。因此本轮不把第三问改动推广到第四问。

## 光学兜底与适用范围

首测三角形包含所有可能真源，后续有效楔形只做相交；no_signal不删区域。覆盖相交多边形的25米方格中心到格内任一点≤25/√2≈17.678<20，枚举包含真源的格必可清除，与朝向无关。

保证依赖静止源、题设闭覆盖、误差界、合法配置和足够现实执行时间。现实HTTP失联与超时不能由几何证明排除。离线模拟器使用浮点秒而官方按微秒累计；本轮零漏源不能替代官方联调。
